

int ft_printf(const char *str, ...)
{
	while (str i)

	if str i == %
	i++
	formatter == str [i]


	if formatter == d
		putnbr va arg (arg, int)
	if format = s
		putstr va arg (char *)
}

printf("bugra %d yasındadır", age);
